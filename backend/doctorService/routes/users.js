var express = require('express');
var router = express.Router();
//var doctorSchema = require('../../db_schema_modules/Doctor'

/* GET users listing. */
router.get('/register', function(req, res, next) {
  
});

module.exports = router;
